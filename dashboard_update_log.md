# 다중 범주 연구 대시보드(Multi-Category Research Dashboard) 업데이트 기록
**진행 일시**: 2026년 4월 22일

---

## 1. 초기 구현 목표 및 문제점
초기에 "학제간 연구(트리맵)"와 "세부 분야(원그래프)" 간의 연결 및 시각화를 개선하려 하였으나, `dashboard_standalone.html` 내부의 독립형 스크립트로 동작하는 구조를 인지하지 못하고 `src/main.js`만 업데이트하면서 수정 사항이 화면에 보이지 않는 문제가 있었습니다. 이를 파악하고 **standalone 스크립트 코드 내에 직접 로직을 이식하는 방식**으로 전면 수정하여 문제를 해결했습니다.

## 2. 주요 개선 사항 (Feature Updates)

### 1) 다중 태깅(Multi-tagging) 논리 전면 개편
- 논문이 여러 대분야(Category)와 세부분야(Sub-category)에 동시에 속할 수 있도록 알고리즘을 개선했습니다. 
- 분류가 안 잡히는 논문이 없도록 초기 Fallback 설정 및 엄밀한 KCI 주제어 매핑을 강화했습니다.

### 2) 세부 테마(Sub-category) 지속적인 맞춤화 및 재구조화
대부분의 논문이 광범위하게 속해 중복 분류가 과도하게 발생하는 현상을 최소화하고자, 카테고리를 더 세밀하고 논리적으로 세분화했습니다.
- **불합리한 명칭 수정**: `불교 및 조명`을 `불교 및 도교 철학`으로 정정했습니다.
- **기존 주제역 분할**: 이형적인 학문이 묶여 있던 `공학/환경 윤리`를 **`환경과 생태 윤리`**와 **`공학과 기술 윤리`**라는 두 개의 별도 범주로 분리하여 분류의 타당성을 확보했습니다.
- **비교철학적 접근**: `선진 유학 (공맹)`, `성리학 및 조선 유학`, `불교 및 도교 철학`, `동서양 사상 비교`
- **이론적/규범적 분석**: `고전 덕 윤리(아리스토텔레스)`, `현대 덕 윤리`, `규범 이론 비교`, `메타 윤리와 정당화`, `도덕 심리학 및 감정`

### 3) 원그래프(Pie Chart) UI 개선 및 선택된 영역 고정 (Selection Lock)
- 원그래프에서 '중복 조각'을 따로 도출하지 않고 세부 분야 파이 조각 텍스트 안에 중복 데이터를 `(영역 중복 N건)`과 같이 삽입하는 방식으로 변경했습니다.
- **클릭 고정 기능 추가**: 원그래프의 조각을 클릭하면 해당 조각이 바깥으로 밀려난 상태로 시각적 고정(`selectedMode: 'single'`)이 되도록 설정했습니다. 이로 인해 내가 현재 어떤 영역을 보고 있는지 명확히 인지할 수 있습니다.
- 글자 잘림 방지를 위해 파이 반지름을 축소하고 `overflow: break` 속성과 컨테이너 폭을 지정하여 배치했습니다.

### 4) 사용자 경험(UX) 최적화: 마우스 오버 툴팁(Tooltip)형 '목록 보기' 버튼 도입
- 기존에는 차트를 누를 때마다 화면 하단 논문 목록으로 즉시 강제 스크롤(scrollIntoView)되어 차트 간 비교 확인이 매우 불편했습니다.
- 이를 개선하고자 처음에 고정형 버튼을 만들었으나 공간 차지 및 직관성 부족 문제가 있어, **최종적으로 '마우스 오버(Hover)' 시 등장하는 ECharts 툴팁 내장형 버튼 도입**으로 UX를 대폭 상향했습니다.
- 사용자가 트라맵 영역이나 원그래프의 특정 조각 위에 마우스를 단순히 갖다 대기만 하면 자연스럽게 `논문 목록 보기` 기능이 포함된 툴팁 말풍선이 따라다니며 나타납니다. 다른 차트 영역을 침범하지 않아 매우 세련된 사용성을 제공합니다.
- 복잡함을 줄이기 위해 버튼 이름에서 괄호 표기 `(n건)` 및 이모티콘 표기를 제거해 진중함을 더했습니다.

### 5) 융합 논문용 별도 산점도 범례 제거
- 버블차트(영향력 시각화)에서 지저분하게 잡히던 '융합 논문' 범례와 데이터를 분리하지 않고, 주(핵심) 분야 하나에 깔끔히 소속되도록 시각화 로직을 축소/정리했습니다.

---

### 6) 툴팁 제거 및 네이티브 라벨/버튼화 (UX 디자인 전면 개편)
- 마우스 커서를 따라다니거나 둥둥 떠다니는 팝업 형태의 툴팁이 오히려 시각을 방해한다는 사용자 피드백을 반영하여 **ECharts 툴팁을 전면 삭제**했습니다.
- 대신 `emphasis` 이벤트와 `rich` 텍스트 서식을 활용하여, **차트 내부의 원래 라벨에서 [논문 목록] 표시가 자연스럽게 스며나오도록** 네이티브 UI로 구현했습니다.
- 원그래프(세부 분야) 레이블은 투명색 더미 텍스트를 기본 뷰에 삽입하여, 마우스를 올렸을 때 레이아웃이 위아래로 재정렬(Layout Shift, 이른바 '짜치는 모션')을 일으키지 않도록 방지했습니다. 
- 버튼은 완전히 단조로운 텍스트가 아니라, 시스템 전역에 쓰이는 KCI 버튼 스타일(Linear Gradient, 그림자 패딩 등)을 이식하여 시각적 일체감을 주었습니다.
- 글자들은 가로/세로 중앙 정렬(`align: center, verticalAlign: middle, position: inside`)뿐만 아니라, **레이아웃 수학적 대칭(상·하단 투명 줄바꿈 밸런싱)**을 엄격히 설정하여 박스 물리적 크기와 상관없이 기하학적 정가운데(대각선 교차점)에 완벽하게 세련된 배치를 띄도록 고강도 보정했습니다.

### 7) 영향력 분석 버블 차트 UX 개선
- 원(버블) 클릭 시, 사용자 화면이 하단 논문 목록 뷰어 영역으로 부드럽게 자동 스크롤(Auto Scroll-Into-View) 되도록 동작 구문을 추가하여 원활한 탐색 흐름을 완성했습니다.

---

### 8) 트리맵 및 원그래프 시각적 완성도 고도화
- **트리맵 라벨 기하학적 중앙 배치**: `rich` 텍스트 사용 시 발생하는 수직 정렬 불균형을 해결하기 위해 일반 텍스트 방식과 `verticalAlign: 'middle'` 조합으로 전환, 모든 박스에서 라벨이 정확한 무게중심에 위치하도록 보정했습니다.
- **원그래프 라벨 가독성 개선**: 
    - `avoidLabelOverlap: true` 설정을 통해 인접한 라벨 간의 겹침을 원천 차단했습니다.
    - 라벨 포맷을 [영역명] + [줄바꿈] + [건수/비율] 2줄 구조로 최적화하여 좁은 공간에서도 텍스트가 잘리지 않도록 개선했습니다.
    - 기존의 강제 width 제약과 `overflow: break` 설정을 제거하여 글자가 부자연스럽게 쪼개지는 현상을 해결했습니다.

### 9) [긴급] 데이터 무결성 복구 및 안정화
- **JSON 손상 수술**: 과거 편집 과정에서 논문 초록 데이터 내부에 자바스크립트 코드가 삽입되어 전체 대시보드가 로딩되지 않던 치명적 오류를 발견하고, 해당 섹션(D. 나르바이스 연구 등)을 정밀 복구하여 628건의 데이터를 정상화했습니다.
- **버전 관리 체계 구축**: 향후 데이터 손상 방지를 위해 작업 전후 `git commit` 인터페이스를 강화하고, 사고 발생 시 `git checkout`을 통한 즉각 복구 프로세스를 가이드라인에 추가했습니다.

---

## 3. 향후 계획 (Next Steps)
- 추가적인 KCI 최신 데이터 엑셀 임포트시의 안정성 평가
- 모바일 뷰어 등 다양한 해상도에서의 대시보드 반응형 레이아웃 최적화
- 데이터 필터링 기능 강화 및 논문 검색 기능 도입 검토
